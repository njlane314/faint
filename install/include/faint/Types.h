#ifndef ANALYSIS_TYPES_H
#define ANALYSIS_TYPES_H

#include <faint/Samples.h>

namespace faint {

using SampleKey = sample::Key;
using SampleOrigin = sample::Origin;
using SampleRole = sample::Role;
using SampleVariation = sample::Variation;

using sample::to_key;

}  // namespace faint

#endif
